//
//  pantallaInicio.swift
//  AppExpo
//
//  Created by CEDAM 11 on 26/11/24.
//

/*import SwiftUI

struct pantallaInicio: View {
    var body: some View {
        ScrollView(.vertical) {
            VStack(spacing: 20) {
                VStack(alignment: .leading) {
                    Text("Sofía")
                        .font(.title)
                        .fontWeight(.bold)
                    Text("No nos atrevemos a muchas cosas porque son dificiles, pero son dificiles porque no nos atrevemos a hacerlas.")
                    
                    Text("-Séneca")
                        .font(.subheadline)
                        .foregroundColor(.gray)
                }
                .padding()
                .background(Color.white)
                .cornerRadius(10)
                .shadow(radius: 5)

                HStack {
                    VStack(alignment: .leading) {
                        Text("Tan solo 30 minutos de ejercicio al dia puede mejorar tu estado de ánimo y reducir los niveles de ansiedad.")
                            .font(.headline)
                        HStack {
                            Image(systemName: "hand.thumbsup")
                            Image(systemName: "hand.thumbsdown")
                        }
                    }
                    .padding()
                    .background(Color.white)
                    .cornerRadius(10)
                    .shadow(radius: 5)

                    VStack(alignment: .leading) {
                        Text("Mantener relaciones saludables es crucial para una buena salud mental.")
                            .font(.headline)
                        HStack {
                            Image(systemName: "hand.thumbsup")
                            Image(systemName: "hand.thumbsdown")
                        }
                    }
                    .padding()
                    .background(Color.white)
                    .cornerRadius(10)
                    .shadow(radius: 5)
                }

                HStack {
                    VStack(alignment: .center) {
                        Text("Fecha 26/11/24")
                            .font(.headline)
                        Text("Añadir pag.")
                            .font(.subheadline)
                        Button(action: {
                        }) {
                            Image(systemName: "plus")
                                .font(.title2)
                                .foregroundColor(.white)
                        }
                        .padding()
                        .background(Color .green)
                        .cornerRadius(10)
                    }
                    .padding()
                    .background(Color.white)
                    .cornerRadius(10)
                    .shadow(radius: 5)

                    VStack(alignment: .leading) {
                        Text("Reflexiones del día")
                            .font(.headline)
                        Text("Hoy me sentí más motivado y productivo.")
                            .font(.subheadline)
                    }
                    .padding()
                    .background(Color.white)
                    .cornerRadius(10)
                    .shadow(radius: 5)
                }
            }
            .padding()
        }
        .navigationTitle("Desplazamiento Vertical")
    }
}*/
